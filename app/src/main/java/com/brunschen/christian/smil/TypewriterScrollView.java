package com.brunschen.christian.smil;

import android.content.Context;
import android.util.AttributeSet;

public class TypewriterScrollView extends TwoDScrollView {

  public TypewriterScrollView(Context context) {
    this(context, null, 0);
  }

  public TypewriterScrollView(Context context, AttributeSet attrs) {
    this(context, attrs, 0);
  }

  public TypewriterScrollView(Context context, AttributeSet attrs, int defStyle) {
    super(context, attrs, defStyle);
  }
}
